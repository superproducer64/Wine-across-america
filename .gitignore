node_modules/
.expo/
dist/
web-build/
ios/
android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
.env
.env.local
.DS_Store
*.pem
npm-debug.*
yarn-debug.*
yarn-error.*
.pnp
.pnp.js


# Google Play service account key (sensitive - never commit)
google-service-account.json

# GCP service account keys, wherever they land (e.g. attached_assets/) —
# these are plain-text credentials and must never be committed
*service-account*.json
pour-across-america-*.json
